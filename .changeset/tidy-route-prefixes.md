---
"@croco/protocols-core": patch
---

Resolve relative route contract paths under their controller prefix while preserving existing full paths without duplicate prefixes. ContractGraph accepts both forms and HTTP routing uses the resolved path.
